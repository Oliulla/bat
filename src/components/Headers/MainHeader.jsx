const MainHeader = ({ title }) => {
    return (
        <h2 className="rounded-t-2xl bg-primary py-4 text-center text-3xl font-bold">
            {title}
        </h2>
    );
};

export default MainHeader;
